У вас есть иерархические или сложные структурированные настройки 
(например, массивы, вложенные объекты), которые неудобно хранить в .env.

Вы используете конфигурацию как код, которую нужно версионировать

тогда использовать config,
в остальных случаях можно брать с .env

